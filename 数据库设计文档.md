# 个人日程管理系统 - 数据库设计文档

## 数据库信息
- 数据库名: schedule_management
- 字符集: utf8mb4
- 排序规则: utf8mb4_unicode_ci

## 表结构

### 1. users (用户表)

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| id | INT | 用户ID | PRIMARY KEY, AUTO_INCREMENT |
| username | VARCHAR(50) | 用户名 | NOT NULL, UNIQUE |
| password | VARCHAR(255) | 密码（bcrypt加密） | NOT NULL |
| email | VARCHAR(100) | 邮箱 | NOT NULL, UNIQUE |
| created_at | TIMESTAMP | 创建时间 | DEFAULT CURRENT_TIMESTAMP |
| updated_at | TIMESTAMP | 更新时间 | ON UPDATE CURRENT_TIMESTAMP |

**索引:**
- idx_username: username字段
- idx_email: email字段

### 2. schedules (日程表)

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| id | INT | 日程ID | PRIMARY KEY, AUTO_INCREMENT |
| user_id | INT | 用户ID | NOT NULL, FOREIGN KEY |
| title | VARCHAR(200) | 日程标题 | NOT NULL |
| description | TEXT | 日程描述 | - |
| start_time | DATETIME | 开始时间 | NOT NULL |
| end_time | DATETIME | 结束时间 | NOT NULL |
| priority | ENUM | 优先级 | ENUM('high', 'medium', 'low') |
| status | ENUM | 状态 | ENUM('pending', 'in_progress', 'completed') |
| created_at | TIMESTAMP | 创建时间 | DEFAULT CURRENT_TIMESTAMP |
| updated_at | TIMESTAMP | 更新时间 | ON UPDATE CURRENT_TIMESTAMP |

**索引:**
- idx_user_id: user_id字段
- idx_start_time: start_time字段
- idx_status: status字段

**外键:**
- user_id -> users(id) ON DELETE CASCADE

## 优先级说明
- high: 高优先级
- medium: 中优先级
- low: 低优先级

## 状态说明
- pending: 待办
- in_progress: 进行中
- completed: 已完成    